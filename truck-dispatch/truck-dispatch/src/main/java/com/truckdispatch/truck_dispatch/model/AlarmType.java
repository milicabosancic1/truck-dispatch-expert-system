package com.truckdispatch.truck_dispatch.model;

public enum AlarmType {
    ESCALATION,
    VEHICLE_STOPPED,
    EMERGENCY_REPLACEMENT,
    FUEL_LEAK,
    DOMINO_ESCALATION,
    OVERLOADED_DRIVER,
    OVERLOADED_TRUCK,
    PROBLEMATIC_ROUTE,
    SHIFT_WARNING,
    IDLE_FLEET
}
